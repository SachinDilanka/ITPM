# Local dev: leave VITE_API_URL empty so axios uses /api and Vite proxies to the backend (vite.config.js).
# Production / preview: set full API base including /api, e.g. https://api.example.com/api
# VITE_API_URL=
